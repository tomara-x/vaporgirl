name: moodygirl

on:
  push:
    paths:
      - 'moodygirl/**'

env:
  # Path to the solution file relative to the root of the project.
  SOLUTION_FILE_PATH: ./moodygirl/Builds/VisualStudio2019/moodygirl.sln

  # Configuration type to build.
  # You can convert this to a build matrix if you need coverage of multiple configuration types.
  # https://docs.github.com/actions/learn-github-actions/managing-complex-workflows#using-a-build-matrix
  BUILD_CONFIGURATION: Release

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
      with:
        submodules: recursive

    - name: add msbuild to path
      uses: microsoft/setup-msbuild@v1.1

    - name: build
      working-directory: ${{env.GITHUB_WORKSPACE}}
      # Add additional options to the MSBuild command line here (like platform or verbosity level).
      # See https://docs.microsoft.com/visualstudio/msbuild/msbuild-command-line-reference
      run: msbuild /m /p:Configuration=${{env.BUILD_CONFIGURATION}} ${{env.SOLUTION_FILE_PATH}}
      
    - name: ziptie
      shell: bash
      run: 7z a -tzip moodygirl.zip -r moodygirl/Builds/VisualStudio2019/x64/Release

    - name: release
      uses: "marvinpinto/action-automatic-releases@latest"
      with:
        repo_token: "${{ secrets.GITHUB_TOKEN }}"
        automatic_release_tag: "moodygirl"
        prerelease: false
        title: "moodygirl"
        files: |
          moodygirl.zip

    # don't need these now
    #- name: save artifact
    #  uses: actions/upload-artifact@v3
    #  with:
    #    name: moodygirl-release
    #    path: moodygirl\Builds\VisualStudio2019\x64\Release

