name: gaiagirl

on:
  push:
    paths:
      - 'gaiagirl/**'
        #- '.github/workflows/gaiagirl.yml' #testing

jobs:
  mac:
    runs-on: macos-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
        with:
          submodules: recursive
      - name: set default scheme
        run: |
          cd gaiagirl/Builds/MacOSX
          scheme_list=$(xcodebuild -list -json | tr -d "\n")
          default=$(echo $scheme_list | ruby -e "require 'json'; puts JSON.parse(STDIN.gets)['project']['targets'][0]")
          echo $default | cat >default
          echo Using default scheme: $default
      - name: build
        env:
          scheme: ${{ 'default' }}
        run: |
          cd gaiagirl/Builds/MacOSX
          if [ $scheme = default ]; then scheme=$(cat default); fi
          if [ "`ls -A | grep -i \\.xcworkspace\$`" ]; then filetype_parameter="workspace" && file_to_build="`ls -A | grep -i \\.xcworkspace\$`"; else filetype_parameter="project" && file_to_build="`ls -A | grep -i \\.xcodeproj\$`"; fi
          file_to_build=`echo $file_to_build | awk '{$1=$1;print}'`
          xcodebuild clean build -scheme "$scheme" -"$filetype_parameter" "$file_to_build" | xcpretty && exit ${PIPESTATUS[0]}
      - name: save artifact
        uses: actions/upload-artifact@v3
        with:
          name: gaiagirl-mac
          path: |
            gaiagirl/Builds/MacOSX/build/Release
  win:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v3
      with:
        submodules: recursive
    - name: add msbuild to path
      uses: microsoft/setup-msbuild@v1.1
    - name: build
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: msbuild /m /p:Configuration=Release gaiagirl/Builds/VisualStudio2019/gaiagirl.sln
    - name: save artifacts
      uses: actions/upload-artifact@v3
      with:
        name: gaiagirl-win
        path: |
          gaiagirl\Builds\VisualStudio2019\x64\Release
  release:
    runs-on: ubuntu-latest
    needs: [mac, win]
    steps:
      - uses: actions/download-artifact@v3
      - name: ziptie
        run: |
          mkdir void
          zip -r gaiagirl-mac-vst.zip gaiagirl-mac/gaiagirl.vst3/
          zip -r gaiagirl-mac.zip gaiagirl-mac/
          zip -r gaiagirl-win.zip gaiagirl-win/
          cp gaiagirl-mac-vst.zip gaiagirl-mac.zip gaiagirl-win/VST3/gaiagirl.vst3 gaiagirl-win.zip void/
      - name: release
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: void/*
          tag: gaiagirl
          overwrite: false
          file_glob: true
          body: "may you find peace"
